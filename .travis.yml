dist: jammy
language: python
python:
  - "3.14"
install:
  - curl -LsSf https://astral.sh/uv/install.sh | sh
  - export PATH="$HOME/.local/bin:$PATH"
  - uv sync --locked --group test
script:
  - uv run pytest
